spring:
  application:
    name: main-service
feign:
  client:
    config:
      default:
        connectTimeout: 50000
        readTimeout: 50000
        loggerLevel: basic
---
spring:
  profiles: dev
  application:
    name: main-service
feign:
  client:
    config:
      default:
        connectTimeout: 50000
        readTimeout: 50000
        loggerLevel: basic
---
spring:
  profiles: qa
  application:
    name: main-service-qa
feign:
  client:
    config:
      default:
        connectTimeout: 50000
        readTimeout: 50000
        loggerLevel: basic
